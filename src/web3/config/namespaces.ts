export enum ContractNamespaces {
	GuestList = 'guestlist',
}

export enum BalanceNamespace {
	Token = 'token',
	Sett = 'sett',
	Geyser = 'geyser',
	GuardedSett = 'guarded',
	NonSettToken = 'nonsetttoken',
}
